<strong><%= link_to entry.date, entry_path(entry) %></strong>
<br>
<%= entry.body%>
<%# <% entry.tasks.each do |task| %> 
  <%# <%= task.body %> 


  <%# <% end %> 

<br>
<%= link_to "Edit", edit_entry_path(entry) %>
<%= link_to "Delete", entry_path(entry), method: :delete, data: { confirm: "Are you sure?" } %>

<br>
<br>